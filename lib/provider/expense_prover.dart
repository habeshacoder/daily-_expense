class TransactionNotifier extemd ChangeNotifier{
  //to do: add the fetching logic here when the user opens the app

  
  //to do: add the putting hive objects here when the user adds a new expense

  //to do: better to add the search and filter functionalities here too!
}
